{
    "ConnectionStrings": {
        "CinemaDb": "Host = localhost; Database = CinemaDb; UserId = postgres; Password = root;",
    }
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    }
}
